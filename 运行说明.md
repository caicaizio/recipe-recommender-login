# 怎么运行（Seasonings 菜谱推荐站 · 带登录版）

一个本地运行的菜谱推荐网站：勾 5 道喜欢的菜，它给你 7 组推荐。右上角可以登录，
登录后会记住你勾选过的菜，推荐越来越准。

---

## 一、最省事：双击启动（推荐）

解压后，进到有 `app.py` 的这一层文件夹，按系统**双击**对应的文件：

| 系统 | 双击这个文件 |
| --- | --- |
| Windows | **`双击启动-Windows.bat`** |
| macOS | **`双击启动-Mac.command`** |
| Linux | **`双击启动-Linux.sh`** |

然后**等着就行**，脚本会自己做完三件事：

1. 建一个独立的 Python 环境（文件夹 `.venv`，只在第一次做）
2. 装需要的包（第一次要几分钟，取决于网速；装不上会自动换国内源重试）
3. 加载菜谱数据，**完成后自动打开浏览器**

### 第一次运行要注意

- **必须联网**（第一次要下载几个包）；之后断网也能开，只是菜谱图片显示不出来。
- **要装 Python 3.9 ~ 3.11**。没装的话脚本会提示你，去
  https://www.python.org/downloads/ 下载，**Windows 安装时务必勾选
  `Add Python to PATH`**，否则双击还是没反应。
- **第一次启动要等 30~60 秒**加载模型，这段时间黑窗口里只有「正在加载菜谱数据…」，
  浏览器还没弹出来 —— 正常的，别关窗口。等看到「浏览器会自动打开 http://…」就好。
  万一没自动弹，手动把 **http://127.0.0.1:5000** 粘到浏览器地址栏。

### 启动后黑窗口能关吗？

不能关，关了网站就停了（不想要了就直接关窗口停止）。
想留着窗口又不挡事，最小化即可。

---

## 二、手动启动（双击脚本出问题时用这个）

```bash
cd 到本项目目录            # 进到有 app.py 这一层

python -m venv .venv       # 建虚拟环境，只做一次
# Windows:      .venv\Scripts\activate
# macOS/Linux:  source .venv/bin/activate

pip install -r requirements.txt
python start.py            # 这个会自动开浏览器；也可以 python app.py（不自动开）
```

> 项目 git 历史里那份 `requirements.txt` 是 2019 年的老版本（Flask 1.0.2 / numpy 1.16.3），
> 现在的 Python 装不上，**已经换掉了**，别用回老的。

`pip install` 慢或失败（多半是网络）：

```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

**5000 端口被占用**（Windows 常见）：换端口启动

```bash
# Windows
set PORT=5001 && python start.py
# macOS/Linux
PORT=5001 python start.py
```

然后访问 http://127.0.0.1:5001

---

## 三、怎么用

1. 首页随机展示 24 道热门菜，勾选 **至少 2 道**（建议 5 道）你喜欢的，点心形图标
2. 点 **Show me what to cook!**
3. 结果页给出 7 组推荐：「相似口味的人也喜欢」「因为你喜欢 X」、
   两个类目推荐、「Make It Again」「Tastebreakers（换个口味）」

## 四、右上角登录

点右上角 **👤 登录 / 注册**：

- 填**昵称 + 口令**（口令至少 4 位）；昵称不存在会自动注册，存在就是登录
- 勾上 **「记住口令」**，下次只要填（或点）昵称，口令会自动补上
- **省份**下拉选一个，下面会立刻显示该地区的主食和口味；登录后页面顶部也会一直显示这一行
  （省份跟着账号存在 `data/accounts.json`，重新登录不丢；不选就是「不指定」，不显示）
- 登录后按钮变成你的昵称，点它可以切换账号、退出登录

⚠️ 口令以哈希形式存在服务端 `data/accounts.json`（PBKDF2-SHA256，不存明文），
但「记住口令」只是存在这台电脑浏览器里的混淆文本，**等同于明文**，
换电脑或把电脑给别人用之前别勾这项。这是本地小站账号：没有找回口令、没有防暴破。

> 登录状态存在 Flask session 里，重启服务后会掉（要重新登录）。
> 想保持就设一个密钥再启动：
> Windows `set SECRET_KEY=一串随机字符 && python start.py`
> macOS/Linux `SECRET_KEY=一串随机字符 python start.py`

---

## 五、双击没反应 / 报错，先看这

| 现象 | 原因和办法 |
| --- | --- |
| 双击 `.bat` 一闪而过什么都没有 | 没装 Python，或装的时候没勾 `Add Python to PATH`。重装 Python 并勾上那一项 |
| 提示「没有 Python」 | 同上，去 python.org 装 3.9~3.11 |
| 卡在装依赖很久 / 报网络错 | 换网（手机热点）重试；脚本第二次会自动换清华源 |
| 黑窗口停了，说创建虚拟环境失败 | 杀毒软件拦截，或 Python 装得不完整；改用第二节的手动启动 |
| 显示「无法访问此页面」 | 服务还在加载，等窗口出现「浏览器会自动打开」再刷新；确认地址是 http://127.0.0.1:5000 |
| 提示端口被占用 | 见第二节换 `PORT=5001` |
| Mac 双击 `.command` 说「无法打开，因为来自身份不明的开发者」 | 在文件上**右键 → 打开**，第一次这样就好 |
| 图片不显示 | 菜谱图片是 allrecipes 的外链，需要联网；断网只是显示占位图，推荐照常 |
| 只勾了 1 道菜就提交 | 会报错，至少勾 2 道 |
| 想重来 | 删掉 `.venv` 目录，重新双击启动脚本 |
