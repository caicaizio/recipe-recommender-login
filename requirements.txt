# 运行本站点需要的包（2026-09 实测可用，Python 3.9 ~ 3.11）。
# 注意：别用 git 历史里那份 2019 年的老 requirements（Flask 1.0.2 / numpy 1.16.3），
# 那些版本在现在的 Python 上装不上。
Flask>=2.2
pandas>=1.5
numpy>=1.24
scipy>=1.10
scikit-learn>=1.2
